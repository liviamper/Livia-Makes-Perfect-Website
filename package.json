{
  "name": "digital-disk",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "postbuild": "node -e \"const fs = require('fs'); if (fs.existsSync('dist/sitemap-0.xml')) { fs.copyFileSync('dist/sitemap-0.xml', 'dist/sitemap.xml'); } else if (fs.existsSync('dist/sitemap-index.xml')) { fs.copyFileSync('dist/sitemap-index.xml', 'dist/sitemap.xml'); }\"",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.1.8"
  }
}
